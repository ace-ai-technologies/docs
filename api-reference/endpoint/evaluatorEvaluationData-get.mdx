---
title: 'Evaluator: GET /evaluation-data'
openapi: 'GET /evaluator/v1/evaluation-data'
---

**Purpose**: Retrieves evaluation data based on query parameters such as `username`, `status`, `limit`, `offset`, and `expertise`.
